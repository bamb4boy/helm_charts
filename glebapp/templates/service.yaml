apiVersion: v1
kind: Service
metadata:
  name: {{ include "glebapp.fullname" . }}
  labels:
    {{- include "glebapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glebapp.selectorLabels" . | nindent 4 }}
