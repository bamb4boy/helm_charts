apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glebapp.fullname" . }}-test-connection"
  labels:
    {{- include "glebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
